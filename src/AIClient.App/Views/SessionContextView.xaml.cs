using System.Windows.Controls;

namespace AIClient.App.Views;

/// <summary>The context report, hosted in a flyout from the chat header.</summary>
public partial class SessionContextView : UserControl
{
    public SessionContextView()
    {
        InitializeComponent();
    }
}
