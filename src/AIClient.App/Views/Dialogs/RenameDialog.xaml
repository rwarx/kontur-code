<ui:FluentWindow x:Class="AIClient.App.Views.Dialogs.RenameDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="Rename chat"
                 Width="440"
                 SizeToContent="Height"
                 ResizeMode="NoResize"
                 ShowInTaskbar="False"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowCornerPreference="Round">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Rename chat" ShowMaximize="False" ShowMinimize="False" />

        <StackPanel Grid.Row="1" Margin="20,8,20,20">
            <TextBlock Text="Chat name" Margin="0,0,0,6" />

            <ui:TextBox x:Name="TitleBox"
                        MaxLength="200"
                        ClearButtonEnabled="True" />

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,18,0,0">
                <!-- IsDefault carries Enter; the text box does not accept returns, so it
                     never swallows the key. -->
                <ui:Button Content="Save"
                           Appearance="Primary"
                           MinWidth="88"
                           Margin="0,0,8,0"
                           IsDefault="True"
                           Click="OnSave" />
                <ui:Button Content="Cancel"
                           MinWidth="88"
                           IsCancel="True" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
