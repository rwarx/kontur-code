using System.Windows.Controls;

namespace AIClient.App.Views;

/// <summary>The tasks surface: pure XAML over its view model, no code of its own.</summary>
public partial class TasksView : UserControl
{
    public TasksView()
    {
        InitializeComponent();
    }
}
